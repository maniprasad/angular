import { SerchDirectiveDirective } from './serch-directive.directive';

describe('SerchDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SerchDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
